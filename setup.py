from distutils.core import setup, Extension

elf_packages = ['elf', 'elf.core']

setup (name = 'elf',
       version = '0.1',
       description = 'dumpelf',
       packages = elf_packages,
       author = "D3M09 X RIKI"
       )

